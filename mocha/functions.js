/* eslint-env node */

module.exports = {

  sumArrayElements: (arr) => {
    // Takes an Array of Numbers as argument
    // Returns the sum of all elements

  },

  sqrtAllArrayElements: (arr) => {
    // Takes an Array of Numbers as argument
    // Returns an array with the square root of all elements

  },

  filterEvenElements: (arr) => {
    // Takes an Array of Numbers as argument
    // Returns an array with only the even elements

  },

  stringIsPalindrome: (str) => {
    // Takes a string as argument
    // Returns true if string is palindrome, else false

  },

  stringContainsOnlyLetters: (str) => {
    // Takes a string as argument
    // Returns true if string contains only letters, else false

  },

  add: (val) => {
    // A function that can be used in a way such as add(2)(5) returns 7
  },

  getIndexArr: (n) => {
    // Using a for loop, create an array of functions which each
    // return its index in the Array
  },
};
